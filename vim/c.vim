" This c.vim bunch of settings is for linux kernel style c. I needed it for
" CS311 and decided it was better to do it this way then any other.
set noexpandtab			"use tabs, not spaces *sigh*
set tabstop=8			"tabstops of 8 spaces in size.
set shiftwidth=8		"indents will be 8 space increments.
set textwidth=78		"80 columns is the general max. 
